APP_ENV=development
LOG_LEVEL=INFO

# Dashboard's connection to Freqtrade's own REST API server (config/config.dryrun.json
# api_server block). These must match that file's username/password.
FREQTRADE_API_URL=http://localhost:8080
FREQTRADE_API_USERNAME=freqtrader
FREQTRADE_API_PASSWORD=changeme
